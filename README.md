# Repository status

This folder is research material, not an application. `requirements.txt` is a
video transcript about utility-site business ideas; despite its filename, it
is not a Python dependency manifest and must not be passed to `pip`.

No product scope, owner, source code, license, data source, or supported entry
point is present. Do not deploy or monetize a clone based only on the claims in
the transcript. An owner must either:

- retain the transcript as research and rename/move it in a separate approved
  cleanup;
- provide an original product brief, data/licensing plan, and implementation;
- archive the folder.
